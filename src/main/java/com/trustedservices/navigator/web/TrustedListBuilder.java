package com.trustedservices.navigator.web;

import com.trustedservices.domain.TrustedList;

/**
 * An interface that collects al the builder. It has been added to facilitate future development of the application
 */
public interface TrustedListBuilder {
    public TrustedList build();
}
